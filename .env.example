# Azure Authentication Configuration
# Required for both backend and frontend authentication

# Azure AD Application Registration
VITE_AZURE_CLIENT_ID=your-app-registration-client-id
VITE_AZURE_TENANT_ID=your-tenant-id-or-common
VITE_AZURE_SUBSCRIPTION_ID=your-azure-subscription-id

# Frontend Configuration
VITE_REDIRECT_URI=http://localhost:5173
VITE_POST_LOGOUT_REDIRECT_URI=http://localhost:5173

# Backend Authentication (Optional - for service principal)
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret
AZURE_SUBSCRIPTION_ID=your-azure-subscription-id

# Backend Configuration
PORT=8080

# Development Settings
NODE_ENV=development
DEBUG=true

# ==============================================================
# SETUP INSTRUCTIONS:
# ==============================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace "your-*" values with your actual Azure credentials
# 3. Get these values from Azure Portal:
#    - Azure AD -> App registrations -> Your app
#    - Subscription ID from Subscriptions page
# 4. For development, you can use Azure CLI authentication
#    instead of setting client secrets
# ==============================================================